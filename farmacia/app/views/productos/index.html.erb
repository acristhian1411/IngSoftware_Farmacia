<p id="notice"><%= notice %></p>
  <section class="tables">   
            <div class="container-fluid">
              <div class="row">
                <div class="col-lg-12">
                  <div class="card">
                   
                  
                    <div class="card-header d-flex align-items-center">
                      <h3 class="h4">Productos</h3>
                    </div>
                    <div class="card-body">

                      <!-- formulario de busqueda -->
  <%= form_tag productos_path, method: :get do %>
      <div class="form-group">
          <%= text_field_tag :prod_descrip, params[:prod_descrip], class: "form-control", placeholder: "Descripcion" %>
      </div>
      <%= submit_tag "Buscar", name: nil, class: "btn btn-primary btn-md" %>
  <% end %>
                      <!-- fin formulario de busqueda -->
                      
                      <div class="table-responsive">  
                        <table class="table table-striped">
  <thead>
    <tr>
      <th>Codigo</th>
      <th>Prod descrip</th>
      <th>Fecha vence</th>
      <th>Cantidad</th>
      <th>Marca</th>
      <th>Seccion</th>
      <th>Tipo med</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @productos.each do |producto| %>
      <tr>
        <td><%= producto.codigo %></td>
        <td><%= producto.prod_descrip %></td>
        <td><%= producto.fecha_vence %></td>
        <td><%= producto.cantidad %></td>
        <td><%= producto.marca.marca_descrip %></td>
        <td><%= producto.seccion.sec_descrip %></td>
        <td><%= producto.tipo_med.tipo_descrip %></td>
        <td><%= link_to 'Mostrar', producto %></td>
        <td><%= link_to 'Editar', edit_producto_path(producto) %></td>
        <td><%= link_to 'Eliminar', producto, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Nuevo Producto', new_producto_path %>
 </div>
                    </div>
                  </div>
                </div>
               
               
                  </div>
                </div>
              </div>
            </div>

            
 
              
          </section>


