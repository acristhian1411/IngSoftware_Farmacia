
    <div class="modal-body">
      <%= form_for :sale_details, url: sale_details_url, method: :post, remote: true do |f| %>
        <div class="row">
          <div class="col-xs-5">
            <div class="form-group">
                <%= f.label :producto_id, class: "sr-only" %>
                <%= f.text_field :producto, placeholder: "Artículo", class: "form-control", value: nil %>
                <%= f.hidden_field :producto_id %>
                <%= hidden_field_tag :prod_descrip  %>
            </div>
          </div>
          <div class="col-xs-2">
            <div class="form-group">
                <%= f.label :cantidad, class: "sr-only" %>
                <%= f.text_field :cantidad, placeholder: "Cant.", class: "form-control" %>
            </div>
          </div>
          <div class="col-xs-2">
            <div class="form-group">
                <%= f.label :precio, class: "sr-only" %>
                <%= f.text_field :precio, placeholder: "Precio", class: "form-control" %>
            </div>
          </div>
          <div class="col-xs-2">
            <div class="form-group">
                <%= f.label :subtotal, class: "sr-only" %>
                <%= f.text_field :subtotal, placeholder: "0", class: "form-control", disabled: true %>
            </div>
          </div>
        </div>
        
          <%= f.submit "Aceptar", class: "btn btn-primary" %>
      <% end %>
    </div>
    <div class="modal-footer">
      <%= link_to "Cancelar", "#", class: "btn btn-danger", "data-dismiss": "modal" %>
    </div>