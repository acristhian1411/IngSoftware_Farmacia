<p id="notice"><%= notice %></p>
  <section class="tables">   
            <div class="container-fluid">
              <div class="row">
                <div class="col-lg-12">
                  <div class="card">
                   
                  
                    <div class="card-header d-flex align-items-center">
                      <h3 class="h4">Secciones</h3>
                    </div>
                    <div class="card-body">

                      <!-- formulario de busqueda -->
  <%= form_tag seccions_path, method: :get do %>
      <div class="form-group">
          <%= text_field_tag :sec_descrip, params[:sec_descrip], class: "form-control", placeholder: "Descripcion" %>
      </div>
      <%= submit_tag "Buscar", name: nil, class: "btn btn-primary btn-md" %>
  <% end %>
                      <!-- fin formulario de busqueda -->

                      <div class="table-responsive">  
                        <table class="table table-striped">
  <thead>
    <tr>
      <th>Sec descrip</th>
      <th>Seccion active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @seccions.each do |seccion| %>
      <tr>
        <td><%= seccion.sec_descrip %></td>
        <td><%= seccion.seccion_active %></td>
        <td><%= link_to 'Mostrar', seccion %></td>
        <td><%= link_to 'Editar', edit_seccion_path(seccion) %></td>
        <td><%= link_to 'Eliminar', seccion, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 <%= link_to 'Nueva Seccion', new_seccion_path %>
</div>
                </div>
              </div>
              </div>
          </div>
         
 </section>



