<p id="notice"><%= notice %></p>

<h1>Listing Seccions</h1>

<div class="col-lg-12">
                <div class="block">
                  <div class="title"><strong>Seccion</strong></div>
                  <div class="table-responsive"> 
                    <table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Sec descrip</th>
      <th>Seccion active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @seccions.each do |seccion| %>
      <tr>
        <td><%= seccion.sec_descrip %></td>
        <td><%= seccion.seccion_active %></td>
        <td><%= link_to 'Show', seccion %></td>
        <td><%= link_to 'Edit', edit_seccion_path(seccion) %></td>
        <td><%= link_to 'Destroy', seccion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
                </div>
              </div>
              </div>
          </div>
 </section>
<br>

<%= link_to 'New Seccion', new_seccion_path %>
