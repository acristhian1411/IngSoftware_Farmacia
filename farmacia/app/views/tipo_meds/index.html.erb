<p id="notice"><%= notice %></p>
  <section class="tables">   
            <div class="container-fluid">
              <div class="row">
                <div class="col-lg-12">
                  <div class="card">
                   
                  
                    <div class="card-header d-flex align-items-center">
                      <h3 class="h4">Tipo de medicamento</h3>
                    </div>
                    <div class="card-body">

                      <!-- formulario de busqueda -->
  <%= form_tag tipo_meds_path, method: :get do %>
      <div class="form-group">
          <%= text_field_tag :tipo_descrip, params[:tipo_descrip], class: "form-control", placeholder: "Descripcion" %>
      </div>
      <%= submit_tag "Buscar", name: nil, class: "btn btn-primary btn-md" %>
  <% end %>
                      <!-- fin formulario de busqueda -->

                      <div class="table-responsive">  
                        <table class="table table-striped">
  <thead>
    <tr>
      <th>Tipo descrip</th>
      <th>Tipo active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_meds.each do |tipo_med| %>
      <tr>
        <td><%= tipo_med.tipo_descrip %></td>
        <td><%= tipo_med.tipo_active %></td>
        <td><%= link_to 'Mostrar', tipo_med %></td>
        <td><%= link_to 'Editar', edit_tipo_med_path(tipo_med) %></td>
        <td><%= link_to 'Eliminar', tipo_med, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>

</table>
<%= link_to 'Nuevo Tipo med', new_tipo_med_path %>
</div>
                </div>
              </div>
              </div> 
          </div>
 </section>



